import rule   from './rule'
import plugin from './plugin'

class Kensho {
  /** rule controller */
  static rule = rule
  /** plugin controller */
  static plugin = plugin
  /**
   * 
   */
  constructor () {
  }
}

export default Kensho